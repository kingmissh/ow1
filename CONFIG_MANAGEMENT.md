# 🏗️ 配置中心化管理体系

基于 **"配置中心化，应用链接化"** 理念，实现 GitHub Codespaces 环境配置的系统化管理。

---

## 📁 目录结构

```
ow1/
├── .config-store/          # ⭐ 受 Git 保护的配置存储
│   ├── openclaw/          # OpenClaw 配置（排除敏感信息）
│   ├── opencode/          # OpenCode 配置
│   └── .gitignore         # 敏感信息排除规则
├── scripts/               # 环境管理脚本
│   ├── manage-config.sh   # 将配置纳入管理
│   ├── setup-env.sh       # 一键恢复环境
│   ├── save-config.sh     # 保存配置更改
│   └── switch-env.sh      # 切换环境分支
└── .devcontainer/         # Codespaces 配置
    └── devcontainer.json
```

---

## 🚀 快速开始

### 首次使用

```bash
# 1. 将现有配置纳入 Git 管理
./scripts/manage-config.sh

# 2. 提交配置
git add .config-store/
git commit -m "feat: init config store"

# 3. 推送到远程
git push
```

### 新建环境/重建 Codespace

```bash
# 自动恢复配置（已在 devcontainer.json 中配置）
./scripts/setup-env.sh

# 手动配置敏感信息（API Key 等）
opencode auth login
```

---

## 🎯 使用场景

### 场景 1：安装新工具并保存

```bash
# 安装工具并配置
npm install -g some-tool
some-tool configure

# 纳入管理
./scripts/manage-config.sh
./scripts/save-config.sh "feat: add some-tool config"
```

### 场景 2：实验失败，回退重来

```bash
# 方法 A：Git 回退
git checkout .config-store/
# 链接已同步更新

# 方法 B：切换分支
git checkout main
./scripts/switch-env.sh main
```

### 场景 3：多环境测试（GPT-4 vs Claude）

```bash
# 创建 Claude 环境
git checkout -b env/claude
# 修改 .config-store/opencode/settings.json
./scripts/save-config.sh "env: configure for Claude"

# 创建 GPT-4 环境
git checkout -b env/gpt4
# 修改配置
./scripts/save-config.sh "env: configure for GPT-4"

# 快速切换
./scripts/switch-env.sh env/claude
./scripts/switch-env.sh env/gpt4
```

### 场景 4：新项目复刻

```bash
# 新账号/新项目
git clone https://github.com/yourname/ow1.git
cd ow1

# 自动恢复配置
./scripts/setup-env.sh

# 手动恢复敏感信息（从 GitHub Secrets）
# 或者重新运行: opencode auth login
```

---

## 📜 脚本说明

### manage-config.sh
将现有配置从 `~/.config/` 移动到 `.config-store/`，并建立软链接。

**用途**: 首次纳入管理或添加新工具配置

### setup-env.sh
从 `.config-store/` 恢复软链接到 `~/.config/`。

**用途**: 重建环境、克隆项目后初始化

### save-config.sh
保存 `.config-store/` 的更改到 Git。

**用途**: 日常保存配置修改

### switch-env.sh
切换 Git 分支并恢复对应配置。

**用途**: 多环境切换

---

## 🔒 安全说明

### 已排除的敏感信息

- `credentials/` - API 密钥
- `identity/` - 身份信息
- `*.key`, `*.pem` - 密钥文件
- `.env` - 环境变量文件

### 恢复敏感信息的方法

1. **GitHub Codespaces Secrets**（推荐）
   - 在 GitHub 仓库设置中添加 Secrets
   - 通过环境变量自动注入

2. **手动配置**
   - 运行 `opencode auth login`
   - 或其他工具的配置命令

3. **本地备份**
   - 个人电脑加密备份 `credentials/` 目录
   - 需要时手动复制到 `~/.openclaw/`

---

## 💡 工作原理

```
┌─────────────────────────────────────────────────────────────┐
│  Git 仓库 (.config-store/)                                   │
│  ├─ openclaw/           ← 配置文件实际存储位置              │
│  ├─ opencode/                                               │
│  └─ .gitignore       ← 排除敏感信息                         │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ 软链接 (Symbolic Link)
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  系统路径 (~/.openclaw/)                                     │
│  → 实际指向 .config-store/openclaw/                         │
│  → 应用程序无感知                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎓 最佳实践

1. **及时保存**: 修改配置后立即运行 `save-config.sh`
2. **分支管理**: 不同实验使用不同分支
3. **敏感信息**: 从不提交 credentials/ 目录
4. **定期推送**: 避免只在本地保存配置
5. **文档记录**: 复杂配置在 commit message 中说明

---

## 🆘 故障排除

### 软链接失效
```bash
./scripts/setup-env.sh
```

### 配置冲突
```bash
# 备份当前配置
mv ~/.openclaw ~/.openclaw.backup

# 重新建立链接
./scripts/setup-env.sh
```

### Git 提交失败
```bash
# 检查敏感信息是否被提交
git status

# 如需撤销
git reset HEAD .config-store/
```

---

*配置管理中心化体系 v1.0*

# OW1 - Sync-Link 配置管理

> 🔄 **Sync-Link**: 修改即同步，回滚即恢复  
> **核心理念**: 你只需要关注 Git，配置自动同步

## ⚡ 极致体验（你的需求已实现）

### 开发时只管 Git
```bash
# 修改配置（通过任意工具界面）
opencode  # 修改设置...

# 查看更改
save              # 别名: git status .config-store/

# 提交更改
save-commit       # 别名: git add + commit

# 推送
git push
```

### 回滚时只管 Git Reset
```bash
# 配置搞砸了？一键回滚
reset-config      # 别名: git checkout .config-store/

# 或者直接用 Git
git reset --hard HEAD^   # 软链接实时同步
```

### 新建环境全自动
```bash
# 1. 打开 Codespace
# 2. init-links.sh 自动运行（postCreateCommand）
# 3. Secrets 自动注入
# 4. 别名自动加载（save, reset-config 等）
# ✅ 环境就绪！
```

---

## 📁 目录结构

```
ow1/
├── .config-store/              # ⭐ 配置保险箱（Git 跟踪）
│   ├── opencode/              # → 链接到 ~/.config/opencode
│   ├── openclaw/              # → 链接到 ~/.openclaw
│   └── .gitignore             # 排除敏感信息
├── scripts/
│   ├── init-links.sh          # 🔧 初始化链接 + 注入 Secrets + 设置别名
│   ├── add-tool.sh            # ➕ 添加新工具到管理
│   └── migrate-to-new-account.sh  # 🔄 跨账号迁移
├── .devcontainer/
│   └── devcontainer.json      # postCreateCommand 自动调用 init-links.sh
├── ENV_SYSTEM_MASTER.md       # 📖 完整文档（架构 + SOP + 故障排除）
└── README.md                  # 📍 本文件（快速开始）
```

## 🚀 快速开始

### 首次配置

```bash
# 1. 安装并配置你的工具
npm install -g opencode
opencode auth login

# 2. 将配置纳入 Git 管理
./scripts/add-tool.sh opencode .config/opencode

# 3. 提交
git add .config-store/ && git commit -m "feat: add opencode config"
git push
```

### 日常使用

```bash
# 修改配置（通过工具界面）
opencode

# 保存到 Git
save-commit

# 回滚（如果搞砸了）
reset-config
```

### 重建环境后

**全自动** - 无需操作：
1. Codespace 重建
2. `init-links.sh` 自动运行
3. 软链接重建 + Secrets 注入 + 别名设置
4. 运行 `source ~/.bashrc` 加载别名

---

## 💡 核心原理

```
┌──────────────────────────────────────────────────────┐
│  Git 仓库 (.config-store/)                            │
│  ├─ opencode/settings.json  ← 物理文件               │
└──────────────────────────────────────────────────────┘
                          │
                          │ 软链接 (Symbolic Link)
                          ▼
┌──────────────────────────────────────────────────────┐
│  系统路径 (~/.config/opencode/)                       │
│  → 实际指向 .config-store/opencode/                  │
│  → 应用程序无感知，实时同步                            │
└──────────────────────────────────────────────────────┘
```

**关键优势**:
- 修改 `~/.config/opencode/` = 修改 `.config-store/opencode/`
- `git status` 立即看到更改
- `git reset` 立即回滚配置

---

## ⌨️ 快捷命令

运行 `source ~/.bashrc` 后可用：

| 命令 | 作用 | Git 等价命令 |
|------|------|-------------|
| `save` | 查看配置更改 | `git status .config-store/` |
| `save-commit` | 提交配置更改 | `git add .config-store/ && git commit` |
| `reset-config` | 回滚配置 | `git checkout .config-store/` |
| `check-links` | 检查软链接状态 | `ls -la ~/.config/opencode` |
| `config-status` | 查看配置仓库状态 | `git status .config-store/` |

---

## 🔒 安全特性

- ✅ **敏感信息排除**: `.gitignore` 过滤 `credentials/`, `identity/`, `*.key`
- ✅ **Secrets 注入**: API Keys 通过 GitHub Codespaces Secrets 自动注入
- ✅ **权限防御**: `chmod 700` 保护 credentials 目录，`chmod 600` 保护密钥文件
- ✅ **分支管理**: 多环境通过 Git 分支切换，无需手动操作

---

## 📖 详细文档

- **[ENV_SYSTEM_MASTER.md](./ENV_SYSTEM_MASTER.md)** - 完整文档包含：
  - 系统架构设计
  - 脚本详细说明
  - 标准操作流程（4 大场景）
  - 跨账号迁移指南
  - 故障排除手册

---

## 🛠️ 脚本说明

### init-links.sh
- **作用**: 初始化软链接、注入 Secrets、设置别名
- **调用**: 自动（postCreateCommand）或手动
- **功能**: 
  - 建立软链接
  - 从 GitHub Secrets 注入 API Keys
  - 设置 `save`, `reset-config` 等别名
  - 设置权限防御

### add-tool.sh
- **作用**: 将新工具配置纳入 Git 管理
- **调用**: 手动（安装新工具后）
- **用法**: `./scripts/add-tool.sh <工具名> <配置路径>`
- **示例**: `./scripts/add-tool.sh opencode .config/opencode`

### migrate-to-new-account.sh
- **作用**: 一键迁移配置到新 GitHub 账号
- **调用**: 手动（跨账号迁移时）
- **用法**: `NEW_GITHUB_USERNAME='name' NEW_REPO_NAME='repo' ./scripts/migrate-to-new-account.sh`

---

## ✅ 验证你的需求

### 需求 1: 开发时只关注当前仓库 Git
**状态**: ✅ 已实现  
**实现方式**: 
- 软链接将配置实时同步到 `.config-store/`
- `save` 别名查看更改
- `save-commit` 别名提交更改

### 需求 2: 回滚时只关注 git reset
**状态**: ✅ 已实现  
**实现方式**: 
- `reset-config` 别名 = `git checkout .config-store/`
- 软链接实时同步，无需重启工具

### 需求 3: 其他操作自动化
**状态**: ✅ 已实现  
**实现方式**: 
- `init-links.sh` 在 postCreateCommand 自动运行
- Secrets 自动注入
- 别名自动设置

---

**状态**: 生产就绪 ✅  
**版本**: v2.0 (Sync-Link 精简版)
